<% var url = ''; if (post.cover){
  url = post.cover.url || post.cover || '';

  if (url.match(/^\/images/)){
    url = config.url + url;
  }

  if (url.match(/^\/\//)){
    url = 'https:' + url;
  }
} %><%= url %>