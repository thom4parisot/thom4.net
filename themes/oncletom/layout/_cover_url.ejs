<% var url = ''; if (page.cover){
  url = page.cover.url || page.cover || '';

  if (url.match(/^\/images/)){
    url = config.url + url;
  }
  
  if (url.match(/^\/\//)){
    url = 'https:' + url;
  }
} %><%= url %>